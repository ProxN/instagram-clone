query GetUserProfile($username: String!) {
  getUserProfile(username: $username) {
    id
    name
    username
    bio
    website
    avatar
    has_followed
    stats {
      followers
      following
      posts
    }
  }
}
