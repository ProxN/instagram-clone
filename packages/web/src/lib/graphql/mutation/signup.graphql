mutation Signup(
  $email: String!
  $password: String!
  $name: String!
  $username: String!
) {
  signup(
    data: {
      email: $email
      password: $password
      name: $name
      username: $username
    }
  ) {
    ...UserResponse
  }
}
